@startuml

title Create Session Sequence

hide footbox
participant "WebRTC App" as App
participant "AT&T API Gateway" as GW

autonumber 
note over App,GW
1. For VTN or NoTN, the Access Token is associated with the VTN or User Name, respectively. 
2. For ICMN, the user has granted consent, and the Access Token was returned to WebRTC app instance. 
3. For ICMN and VTN, an e911 address identifier is set.

end note 

App->GW:POST /sessions (AT,services[,e911Id])
GW-->App:201  Created (sessionId, expires)


@enduml