@startuml

title Leave chat Group Sequence
hide footbox
participant "Endpoint A" as EPA
participant "AT&T API Gateway" as Gateway
participant "Endpoint B" as EPB

autonumber
note over EPA, EPB
1. EndPoint A and EndPoint B are registered with Gateway
2. Both EndPoints have established event channel to get notification of network events. 

end note

loop Long polling 
EPA->Gateway: GET /sessions/{sessionId}/events
end

EPA -> Gateway:DELETE /sessions/{sessionId}/groups/{groupId}
Gateway-->EPA: 204 | No Content
autonumber 2


autonumber 
loop Long polling 
EPB->Gateway:  GET /sessions/{sessionId}/events
Gateway-->EPB:200 OK (type=groups, subtype=info)
end

@enduml