@startuml

title Accept | Reject Group Chat Invitation | Join Ongoing Group Chat Sequence
hide footbox
participant "Endpoint A" as EPA
participant "AT&T API Gateway" as Gateway
participant "Endpoint B" as EPB
autonumber
note over EPA, EPB
1. EndPoints are registered with Gateway
2. EndPoints have established event channel to get notification of network events. 

end note

loop Long polling 
EPA->Gateway: GET /sessions/{sessionId}/events
Gateway-->EPA: 200 OK Event(type=groups, subtype=invite)

end

EPA -> Gateway:PUT /sessions/{sessionId}/groups/{groupId}(accept|decline|join)
Gateway-->EPA: 204 | No Content

autonumber 

loop Long polling 
EPB->Gateway: GET /sessions/{sessionId}/events
Gateway-->EPB: 200 OK Event(type=groups, subtype=info)

end
@enduml